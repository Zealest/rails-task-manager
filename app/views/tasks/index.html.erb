<h1>Task List</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.title %> - <%= link_to 'View Details', task_path(task) %>
    </li>
  <% end %>

  <%= link_to 'Delete', delete_task_path(task), method: :delete, data: { confirm: 'Are you sure?' } %>
</ul>
